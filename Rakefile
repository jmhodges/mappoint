# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/mappoint.rb'

Hoe.new('mappoint', Mappoint::VERSION) do |p|
  # p.rubyforge_name = 'mappoint' # if different than lowercase project name
  p.developer('Jeff Hodges', 'jeff@somethingsimilar.com')
end

# vim: syntax=Ruby
